{
  "name": "hookminer",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n server,client -c blue,green \"PORT=3001 npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "postinstall": "npm run install:all"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
