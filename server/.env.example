# Default: DeepSeek. Just set your key and leave the rest commented out.
DEEPSEEK_API_KEY=

# Optional: point at any other OpenAI-compatible API instead (OpenAI,
# Gemini's OpenAI-compat endpoint, etc.) by uncommenting and setting these.
# API_KEY takes priority over DEEPSEEK_API_KEY when both are set.
# API_BASE_URL=https://api.openai.com/v1
# API_KEY=
# MODEL=gpt-4o-mini
